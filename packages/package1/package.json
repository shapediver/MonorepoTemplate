{
  "name": "@shapediver/package1",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "keywords": [],
  "author": "Michael Oppitz <michael@shapediver.com>",
  "license": "ISC",
  "main": "dist/index.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shapediver/ShapeDiverMonorepoTemplate.git"
  },
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "check": "tsc --noEmit",
    "build": "tsc -b",
    "build-dep": "lerna run build --stream --scope=$npm_package_name --include-dependencies",
    "build-dev": "rm -rf ./dist-dev && mkdir dist-dev && cp index.html dist-dev/index.html && webpack serve --config ../../webpack.dev.js --output-filename bundle.js --output-path dist-dev --content-base ./dist-dev",
    "build-prod": "rm -rf ./dist-prod && mkdir dist-prod && cp index.html dist-prod/index.html && webpack --config ../../webpack.prod.js --output-filename bundle.js --output-path dist-prod"
  },
  "bugs": {
    "url": "https://github.com/shapediver/ShapeDiverMonorepoTemplate/issues"
  },
  "homepage": "https://github.com/shapediver/ShapeDiverMonorepoTemplate#readme",
  "typings": "dist/index.d.ts",
  "devDependencies": {
    "@shapediver/ts-config": "^1.0.0",
    "lerna": "^3.22.1",
    "typescript": "^4.1.2",
    "webpack": "^5.6.0",
    "webpack-cli": "^4.2.0",
    "webpack-dev-server": "^3.11.0"
  }
}
